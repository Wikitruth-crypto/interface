
/**
 * Default transaction confirmations
 */
export const DEFAULT_CONFIRMATIONS = 1;

/**
 * Transaction timeout time (milliseconds)
 */
export const TRANSACTION_TIMEOUT = 60000; // 60 seconds

/**
 * Polling interval (milliseconds)
 * Used to query transaction status or blockchain data
 */
export const POLLING_INTERVAL = 4000; // 4 seconds
